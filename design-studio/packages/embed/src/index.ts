/**
 * `@studio/embed` — der Design-Baustein für fremde Seiten.
 *
 * Ein Custom Element ohne Rahmenwerk, damit es in eine Wix-Seite genauso geht
 * wie in eine React-Anwendung, ohne dort etwas zu erzwingen. Wer das Paket
 * einbindet, ruft `registriere()` und setzt `aussage` und `ausspielungen`.
 */

export * from './element.js';
